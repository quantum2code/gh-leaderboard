{
  "name": "gh-leaderboard",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "check-types": "pnpm -r check-types",
    "dev:web": "pnpm --filter web dev",
    "db:push": "pnpm --filter @gh-leaderboard/db db:push",
    "db:studio": "pnpm --filter @gh-leaderboard/db db:studio",
    "db:generate": "pnpm --filter @gh-leaderboard/db db:generate",
    "db:migrate": "pnpm --filter @gh-leaderboard/db db:migrate",
    "db:local": "pnpm --filter @gh-leaderboard/db db:local"
  },
  "dependencies": {
    "@gh-leaderboard/env": "workspace:*",
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@gh-leaderboard/config": "workspace:*",
    "@types/node": "^22.13.14",
    "typescript": "^5"
  },
  "packageManager": "pnpm@10.23.0"
}
